//! Reference family: variables, member references, global references,
//! aliases, conditionals, derived types. Not implemented yet.

use crate::ElementId;

pub fn refines(_input: ElementId, _container: ElementId) -> bool {
    false
}
